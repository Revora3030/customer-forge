# Finish every remaining Customer Forge upgrade

## Goal
Complete the remaining work from the 58-item program without weakening tenant isolation, authentication, billing, publishing, custom domains, analytics, versions, rollback, or factual safeguards.

The finished system will create a complete, materially distinctive, editable multi-page website with Revora’s native engine, keep customer facts and exact wording protected, and refuse to call a preview ready until real browser evidence passes.

## Verified starting point

- Customer website planning and first-build writing are now native-only; outside model orchestration and writing were removed from that path.
- Luna requires explicit operator enablement, external-AI enablement, native-only mode to be disabled, a configured key, and the existing hard global budget.
- Native first-build synthesis now records strategy stages, page jobs, fact provenance, literal text locks, review findings, and rationale before materialization.
- The current visual check measures real rendered pages in the owner’s browser and is re-graded on the server; publishing already rejects missing or stale measured evidence.
- The first-build worker still records browser, visual, mobile, and performance as `NOT_VERIFIED`; automatic browser measurement and repair are not yet part of first-build completion.
- Visual editing, drafts, versions, and undo/redo exist, but granular undo history does not survive reload. Image controls and side-by-side review remain incomplete.
- A Worker cannot run a local headless browser. Automatic rendering will therefore run honestly in the signed-in owner’s browser unless a separately verified browser-rendering service is configured.

## Batch 1 — Finish intelligence, facts, and safety

1. Keep native-only customer builds and literal edits as permanent tested boundaries.
2. Expand the native synthesis record into industry-weighted specialist reviews for facts, links, security, accessibility, SEO, conversion, content depth, and visual coherence.
3. Preserve language, exact wording, rejected styles, design locks, and customer-authored fields through redesign and regeneration.
4. Add durable field-level provenance and field locks to the existing content model, with organization-scoped access controls and safe migration behavior.
5. Reject malformed plans and unsafe mutations before writing; preserve linked pages and unaffected sections during regeneration.
6. Validate contact, phone, quote, booking, menu, and call-to-action destinations against real configured pages and workflows.
7. Keep outside-provider capability checks operator-only. Add provider/model/task/tenant isolation, bounded retry rules, catalogue quarantine, quota forecasting, concurrency controls, deduplication, and honest status reporting.
8. Make Luna advisory-only with tenant-aware allocations inside the unchanged hard global $20 monthly cap; preserve atomic reservation, settlement, no auto-top-up, and no paid fallback.

## Batch 2 — Complete first-build quality

9. Make the full deterministic sequence explicit in saved evidence: intake → industry intelligence → buyer intent → conversion strategy → creative direction → fingerprint → sitemap → content → imagery → composition → materialization.
10. Produce the appropriate home, services, about, contact, and supporting intent pages with unique jobs, search intent, conversion actions, and useful truthful depth.
11. Ensure navigation and contextual links reach every public page; prevent stranded or misleading destinations.
12. Apply the persisted fingerprint to every shell, navigation, hero, section order, grid, type system, spacing system, surface, border, radius, background, media treatment, footer, transition, and action pattern.
13. Add cross-industry and same-industry fixture comparisons that fail when materially different briefs converge on the same composition contract.
14. Use customer imagery first, then correctly licensed free stock, then only a live-verified genuinely free generator, then truthful abstract artwork or an explicit placeholder.
15. Add duplicate-image detection, useful alt text, decorative-image classification, source/licence provenance, crop and focal-point metadata.
16. Keep unsupported testimonials, credentials, awards, prices, results, statistics, guarantees, integrations, and business-specific photography blocked. Missing required facts remain `UNKNOWN` or produce an honest owner prompt.

## Batch 3 — Enforce render, review, repair, and readiness

17. Replace the decorative first-preview record with the existing real readiness source of truth.
18. After a successful first build, automatically offer and run browser measurement once while the authorized owner has the builder open; never imply server-side browser work occurred.
19. Measure every generated page at exactly the required 320, 375, 390, 414, 768, 1024, 1280, and 1440 widths, while retaining any useful wider diagnostic checks separately.
20. Record overflow, clipping, overlap, sticky headers, long words, touch targets, keyboard/focus behavior, landmarks, headings, labels, contrast, reduced motion, console errors, network failures, and failed resources.
21. Record real Core Web Vitals, response timing, image/font/script weight, request count, loading priorities, and page-specific budgets; use `NOT_VERIFIED` whenever evidence cannot be collected.
22. Check unique metadata, canonical behavior, Open Graph, safe structured data, sitemap coverage, indexability, internal links, conversion continuity, and factual completeness.
23. Map findings to supported repair kinds and editable elements. Create a restore point, apply only bounded safe repairs, rerender, remeasure, and keep the repair only when evidence improves.
24. Cap repair loops, surface unresolved blockers, invalidate stale evidence after every content change, and prevent all ready/completion language while required evidence is failed, blocked, stale, or missing.
25. Persist organization-scoped per-page/per-viewport measurements, repairs, before/after evidence, final verdict, and reviewer participation with explicit grants, row security, indexes, and retention.

## Batch 4 — Finish the preview-first builder

26. Stream compact build stages into the live preview and collapse completion into a calm evidence summary.
27. Link each quality finding to its page and editable element with one plain-language repair action.
28. Persist granular undo/redo history per organization so Apply, Cancel, Undo, Redo, and rollback remain available after reload without crossing tenants.
29. Add direct image upload, licensed stock search, crop, focal point, alt text, replace, remove, and provenance controls to visual editing.
30. Keep AI picture editing disabled unless a genuinely zero-cost edit-capable model passes a live capability test.
31. Add side-by-side before/after review for native and manual changes, retaining draft branches, restore points, versions, publish blocks, and exact change attribution.
32. Add touch-safe mobile selection and reordering alongside the existing desktop interactions.

## Batch 5 — Durable proof and release

33. Add browser journeys for fresh onboarding, generation, five-page navigation, all eight widths, direct editing, persistence, reload-safe rollback, image fallback, first-preview gating, repair, draft restore, and publishing safeguards.
34. Add fixtures across materially different industries and adversarial inputs covering unsupported facts, exact wording, malformed plans, unsafe links, provider failures, zero-cost enforcement, and repeated imagery.
35. Test organization isolation and privilege boundaries against the live backend using separate authorized contexts; verify no secrets or cross-tenant evidence are exposed.
36. Run targeted tests, the complete suite, strict types, lint/build checks, dependency and security scans, authenticated browser journeys, generated-site rendering, console/network review, and available provider checks.
37. Re-run every failed check after repair. Update the roadmap from measured evidence only and report the exact revision, changed files, test totals, browser widths, security result, provider-routing status, and reproducible blockers.

## Technical safeguards

- Reuse the existing worker, content model, visual measurement, server re-grading, production readiness, repair evidence, branches, snapshots, and version history instead of adding competing systems.
- All new backend records are append-only or safely mutable, organization-scoped, explicitly granted, row-secured, indexed, and backward-compatible.
- Customer content, screenshots, and attachments never enter an outside model during website creation.
- External-provider diagnostics remain isolated from customer builds and cannot write, approve, repair, or publish a website.
- Browser evidence is never fabricated. Client measurements are validated and re-graded server-side before they affect readiness.

## Completion criteria and honest blockers

- A fresh customer receives a complete, truthful, distinct, editable multi-page site and cannot see a ready state until every required rendered check passes.
- No unsafe repair survives without improved rerendered evidence; no protected customer wording or fact changes without authorization.
- All required widths, pages, navigation paths, accessibility checks, SEO checks, performance checks, and conversion destinations have saved evidence.
- Tenant isolation, auth, billing, fixed pricing, publishing, custom domains, analytics definitions, drafts, versions, and rollback remain intact.
- AI picture editing remains visibly unavailable until a genuinely free edit-capable provider succeeds in a live probe; it will not be simulated.
- Live email, CRM, and payment-provider delivery checks remain blocked until the corresponding accounts are connected; local contracts and failure behavior will still be tested.
- The nested dependency advisory remains reported until a compatible upstream update resolves it; it will not be hidden, suppressed, or “fixed” by breaking the framework.