# Builder 10/10 — real full-site benchmark and upgrade

## Outcome

Build a complete representative customer website through Revora’s real builder, compare its rendered desktop and mobile result against a polished Lovable-style standard, then repair the parts that look generic, repetitive, slow, or unfinished.

## What I’ll change

- **Real benchmark build:** create an isolated draft/fixture using supplied, truthful business facts; run the same planning, apply, preview, and rendering path customers use; preserve all existing customer websites.
- **Visual quality:** strengthen page composition, typography scale, section rhythm, media treatment, backgrounds, cards, calls to action, and mobile layouts so design choices visibly survive into the public site.
- **Industry variation:** make layouts, section variants, imagery slots, palettes, type, and motion vary coherently by business and design fingerprint instead of converging on one template.
- **Builder reliability:** verify every planned action is persisted and visible, make partial application unmistakable, refresh the preview only after writes settle, and expose a clear before/after result without fake success.
- **Content architecture:** improve complete-site page and section planning while preserving exact facts and never inventing reviews, credentials, prices, or results.
- **Regression protection:** add focused tests for distinct output, renderer execution, mobile-safe composition, and applied-action reporting.

## Verification

- Capture before/after screenshots at phone, tablet, laptop, and wide desktop widths.
- Check overflow, visual hierarchy, links, console/runtime errors, and the real persisted public preview.
- Run the full test suite, type checks, security/free-AI/repository audits, and production build.
- Keep payments, authentication, tenant isolation, publishing, custom domains, analytics, and existing sites unchanged.

## Technical details

The work stays inside the existing structured `AgentAction` pipeline and public section renderer. No paid AI path, new provider, destructive migration, or external key is required. The benchmark uses an isolated test path or pure fixture where production-safe isolation cannot be proven.
